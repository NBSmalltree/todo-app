# TodoFloat — 桌面智能待办清单

## 申报类型说明

**架构类创新 + 方法流程类创新** — TodoFloat 是一款基于 Electron 多窗口架构的桌面悬浮待办管理系统，融合大语言模型实现任务的自动分类与工作智能分析，支持子任务分解、番茄钟专注计时和任务的就地编辑。**其核心创新在于将碎片化的任务记录转化为结构化的工作洞察，构建了从"记录"到"优化"的效率管理闭环。**

## 一、背景介绍

在日常研发和项目管理工作中，任务管理是保障工作效率的核心环节。目前常见的任务管理工具存在以下几类问题：

1. **工具与工作流程脱节**：多数待办清单应用以独立窗口运行，用户需要频繁在任务工具和工作窗口之间切换，打断工作思路。尤其在多屏幕、多任务并行的开发场景中，来回切换窗口的成本不可忽视。

2. **任务数据缺乏深度利用**：传统待办工具仅提供"添加—完成—删除"的基本生命周期，完成的任务往往直接丢弃或仅简单归档，无法从中提炼出工作模式、时间分配、效率趋势等有价值的信息。用户难以回答"这周我主要在做什么""哪类工作投入过多或不足"等关键问题。

3. **任务分类依赖人工**：归档后的任务需要用户手动打标签或分类，增加了操作成本，导致大多数用户放弃分类，历史数据逐渐变成"死数据"，无法被有效检索和分析。

4. **工具碎片化**：任务记录在一个应用，工作日志在另一个应用，时间统计又在第三个应用。数据分散在不同工具中，难以形成统一的工作视图。

5. **任务编辑繁琐**：修改已有任务内容通常需要通过右键菜单或专门的编辑入口，操作步骤多，打断工作思路。快速修改任务内容的需求难以得到满足。

6. **任务记录难以转化为工作洞察**：传统待办工具以"记录—完成"作为唯一闭环，用户记录了大量的任务数据，却缺乏从中提炼工作模式、识别效率瓶颈的能力。工具始终停留在"记录者"的角色，无法升级为"洞察者"。

针对上述问题，我们设计并实现了 TodoFloat——一款悬浮于所有窗口之上的桌面智能待办清单，通过"始终可见 + AI 自动分类 + 数据驱动分析 + 就地编辑"的方式，将任务管理融入日常工作流。更进一步，通过"任务记录→子任务拆解→番茄钟执行→归档分类→数据分析→优化建议"的完整闭环，TodoFloat 不仅是任务记录工具，更是一个驱动效率持续提升的工作方法论载体。

【图片：TodoFloat 悬浮窗口实际使用场景截图——悬浮在 IDE/浏览器等应用上方的待办清单窗口，展示其"始终置顶"的效果】

## 二、解决方案

### 1. 概括描述如何实现

TodoFloat 采用 Electron 桌面应用框架，构建了一个**多窗口协作**的架构体系：主窗口以"始终置顶 + 透明悬浮"的方式覆盖在其他应用之上，用户无需切换窗口即可快速记录和管理任务；通过快速筛选栏和搜索面板，用户可按全部/今天/过期/无截止日期一键切换视图，快速定位关注的任务；支持任务的双击就地编辑，大幅降低编辑摩擦成本。归档与分析功能通过独立窗口承载，由系统托盘统一入口管理。在此基础上，通过集成 OpenAI 兼容协议的大语言模型 API，实现了任务归档时的**自动分类**和**工作数据智能分析**两个核心能力，并构建了"任务记录→子任务拆解→番茄钟执行→归档分类→数据分析→优化建议"的完整工作闭环，将孤立的"添加—完成"操作升级为驱动个人效率持续提升的系统性方法论。

### 2. 方案具体实现步骤

#### 整体架构

TodoFloat 采用经典的 Electron 主进程/渲染进程分离架构，整体分为四层：

```
┌─────────────────────────────────────────────────────────┐
│                    渲染进程（React 18）                    │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐     │
│  │  TodoWindow   │ │  TrayView    │ │  Settings     │     │
│  │  悬浮待办窗口  │ │  归档+分析页  │ │  设置页面     │     │
│  └──────┬───────┘ └──────┬───────┘ └──────┬───────┘     │
│         └────────────────┼────────────────┘              │
│                    preload.js (IPC 桥接)                  │
├─────────────────────────────────────────────────────────┤
│                    主进程（Electron）                      │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐     │
│  │   main.js     │ │  database.js │ │   llm.js      │     │
│  │ 窗口管理/IPC  │ │ SQLite 存储  │ │ LLM 集成      │     │
│  └──────────────┘ └──────────────┘ └──────────────┘     │
├─────────────────────────────────────────────────────────┤
│  构建层：Vite 6 + electron-builder + GitHub Actions      │
└─────────────────────────────────────────────────────────┘
```

1. **渲染进程层**：React 18 + Tailwind CSS 构建，包含三个独立页面组件：
   - TodoWindow：悬浮待办窗口，承载任务交互、筛选搜索与番茄钟；
   - TrayView：归档分析页，承载历史归档查看与工作数据分析；
   - Settings：设置页面，承载 API Key、主题与番茄钟配置。
   三组件通过 Hash 路由切换，共享 preload 脚本暴露的 IPC 接口。

2. **桥接层**：preload.js 通过 `contextBridge.exposeInMainWorld` 将 Electron 的 IPC 通信封装为 `window.electronAPI` 对象，实现渲染进程与主进程之间的安全通信。

3. **主进程层**：由三个核心模块组成：
   - main.js：多窗口生命周期管理、IPC 事件分发与番茄钟状态机；
   - database.js：SQLite 操作封装，管理 todos / subtasks / pomodoro_sessions 三张核心表；
   - llm.js：大语言模型调用封装，统一 OpenAI 兼容协议的请求与响应处理。

4. **构建层**：Vite 6 负责前端资源构建，electron-builder 负责跨平台安装包打包，GitHub Actions 实现 macOS / Windows 双平台自动构建与发布。

【图片：应用整体模块交互示意图——展示三个窗口（悬浮窗、归档窗、设置窗）通过系统托盘入口的关系，以及与 SQLite 数据库和 LLM 服务之间的数据流向】

#### 数据库设计

应用基于 SQLite 存储，启动时开启 WAL 模式以保证并发读写性能，核心数据模型如下：

1. **todos — 任务主表**
   - 核心字段：text（内容）、completed / completed_at（完成状态）、archived / archived_at（归档状态）、category（AI 分类标签）、sort_order（排序序号）
   - 软删除机制：标记 `deleted=1` 而非物理删除，支撑撤销功能

2. **subtasks — 子任务表**
   - 关联字段：todo_id（外键，关联父任务）
   - 核心字段：text（内容）、completed（完成状态）、sort_order（排序序号）
   - 加载策略：首次展开时从数据库懒加载，由本地 Set 管理展开/折叠状态

3. **pomodoro_sessions — 番茄钟记录表**
   - 关联字段：task_id / task_text（关联任务）
   - 核心字段：duration（计划时长）、actual_duration（实际执行时长）、cycle_type（专注/短休息/长休息）、completed（完成状态）
   - 统计支撑：工作分析页面的今日/本周/本月完成数及总专注时长计算

4. **settings — 配置表**
   - 键值对结构：key / value
   - 存储项：API Key 及模型配置、主题偏好、番茄钟时长参数、窗口位置与尺寸

```
                    ┌──────────────────────────────────┐
                    │             todos                  │
                    │           (任务主表)                │
                    │  id (PK, 主键)                    │
                    │  text (任务内容)                   │
                    │  category (AI 分类标签)            │
                    │  completed_at (完成时间)           │
                    │  archived_at (归档时间)            │
                    │  sort_order (排序序号)             │
                    │  deleted (软删除标记)              │
                    └──────┬──────────────┬─────────────┘
                           │1             │1
                      todo_id         task_id
                   (FK → todos.id) (FK → todos.id)
                           │N             │N
           ┌───────────────┘               └──────────────┐
           ▼                                              ▼
┌─────────────────────────┐               ┌──────────────────────────┐
│        subtasks          │               │    pomodoro_sessions      │
│       (子任务表)          │               │      (番茄钟记录表)        │
│  id (PK, 主键)           │               │  id (PK, 主键)           │
│  todo_id (FK → todos.id)│               │  task_id (FK → todos.id)│
│  text (子任务内容)        │               │  task_text (任务快照)     │
│  completed (完成状态)     │               │  duration (计划时长, 秒)  │
│  sort_order (排序序号)    │               │  actual_duration (实际)  │
│  created_at (创建时间)    │               │  cycle_type (周期类型)   │
└─────────────────────────┘               │  completed (完成状态)     │
                                          └──────────────────────────┘

                    ┌──────────────────────────────────┐
                    │            settings               │
                    │           (配置表)                 │
                    │  key (PK, 配置键名)               │
                    │  value (配置值)                   │
                    └──────────────────────────────────┘
```

表间关系：todos 作为核心枢纽，通过 `todo_id` 一对多关联 subtasks（子任务完成进度由子记录实时聚合计算），通过 `task_id` 一对多关联 pomodoro_sessions（番茄钟统计由 session 记录按时间维度汇总生成）。settings 为独立键值对表，无外键关联。

#### 具体实现步骤

**步骤一：悬浮窗口的创建与管理**

应用启动后，主进程执行以下操作：

1. 初始化 SQLite 数据库，创建 todos 表和 settings 表，开启 WAL 模式提升并发读写性能；
2. 从数据库读取上次保存的窗口位置和尺寸（`window_bounds`），校验该位置是否在当前可见屏幕区域内（支持多显示器断开后的自动重置）；
3. 创建 BrowserWindow，配置 `alwaysOnTop: true`、`frame: false`、`transparent: true`、`skipTaskbar: true`，使窗口悬浮于所有应用之上且不在任务栏显示；
4. 设置窗口层级为 `'floating'`（而非更高优先级的 `'screen-saver'`），确保中文输入法候选窗口能正常显示在悬浮窗之上；
5. macOS 下启用 `setVisibleOnAllWorkspaces(true, { visibleOnFullScreen: true })`，使窗口在所有桌面空间和全屏应用中可见；
6. 禁用 Chromium 原生的 Ctrl+滚轮缩放，由渲染进程接管缩放逻辑；
7. 创建系统托盘，注册右键菜单（待办清单 / 历史归档 / 设置 / 退出），点击托盘图标弹出菜单；
8. 应用启动时通过 `app.requestSingleInstanceLock()` 获取单实例锁，若获取失败（已有实例在运行）则立即退出，避免 Windows 下多次双击导致���个进程和多个托盘图标共存的问题。当用户尝试启动第二个实例时，已有实例通过 `second-instance` 事件自动将悬浮窗激活并聚焦到前台；
9. 通过 IPC 事件广播实现多窗口间的数据同步：归档操作后通知归档窗口刷新（`data-changed`），恢复待办后通知悬浮窗刷新，主题和透明度变更时向所有存活窗口广播对应事件，保证各窗口数据实时一致。

【图片：系统托盘右键菜单截图——展示"待办清单、历史归档、设置、退出"四个菜单项】

**步骤二：窗口缩放机制**

用户可通过两种方式缩放悬浮窗口：

- **Ctrl/Cmd + 滚轮**：渲染进程监听 wheel 事件，根据 deltaY 值计算缩放增量（鼠标滚轮通常 ±100/120，触控板为较小值），通过 IPC 调用主进程的 `applyScaleToWindow` 方法，保持窗口中心点不变地进行等比缩放。
- **四角拖拽**：在窗口四角放置透明的拖拽热区（16x16 像素），监听 mousedown/mousemove/mouseup 事件，根据对角线方向的移动距离计算缩放比例。

缩放范围限制在 0.3x 至 2.5x 之间，使用 CSS `zoom` 属性实现前端内容的同步缩放，配合 `transition: zoom 0.15s ease-out` 实现平滑过渡。

【图片：窗口缩放效果对比图——同一悬浮窗口在 0.5x、1.0x、2.0x 三个缩放比例下的并排截图，展示等比缩放效果】

**步骤三：任务交互系统**

TodoFloat 围绕任务项提供了完整的交互操作，涵盖从基础操作到高级交互的各个层面：

1. **添加任务**：用户在输入框输入任务内容，按回车提交（通过 `isComposingRef` 检测中文输入法组合状态，避免输入过程中误提交）。主进程执行 `INSERT INTO todos`，返回新记录，前端为其添加 `todo-enter` CSS 动画（opacity 0→1，translateY -8px→0，scale 0.96→1）。
2. **标记完成**：点击左侧圆圈，触发 `todo-check-animate` 脉冲动画（scale 1→1.25→1），主进程更新 `completed` 和 `completed_at` 字段。已完成任务自动排列到底部并以分隔线区分，数量较多时默认折叠为"已完成 N 项"折叠栏，点击可展开查看，不占据主要列表空间。
3. **删除与撤销保护**：先播放 `todo-exit` 滑出动画（translateX 0→30px，opacity 1→0，max-height 收缩至 0），等待 200ms 动画完成后执行数据库软删除（标记 `deleted=1`，保留数据用于恢复）。删除后底部弹出深色通知条，显示"已删除"及"撤销"按钮，用户可在 5 秒内撤销恢复。归档操作同样支持该撤销机制。
4. **颜色标签**：任务左侧显示颜色圆点指示器，点击可循环切换红/橙/黄/绿/无五种状态，直观标记紧急程度。
5. **右键菜单**：右键点击任务弹出上下文菜单，提供"标记为已完成/恢复为待办""归档""删除"等操作选项。仅已完成任务允许归档。
6. **拖拽排序**：未完成的任务支持上下拖拽重新排列。每项左侧显示 6 点拖拽手柄（hover 时可见），拖拽时被拖项变为半透明，目标位置显示天蓝色指示线。放下后通过事务批量更新数据库中的 `sort_order` 字段，实现排序的持久化存储。新添加的任务自动排在列表最前。
7. **双击编辑**：双击任务文本即可进入编辑模式，文本变为输入框并自动聚焦。编辑完成后按 Enter 保存，按 Esc 取消编辑，点击其他地方自动保存。支持中文输入法（IME），编辑过程中禁用拖拽功能以避免冲突。空文本会自动取消编辑，防止误删任务内容。

【图片：悬浮窗口右键菜单截图——对一条已完成任务右键弹出的上下文菜单，展示"恢复为待办""归档""删除"三个选项】

**步骤四：快速筛选与搜索系统**

TodoFloat 实现了"一键快筛 + 多条件精搜"的两��筛选体系：

1. **一级快筛 — 快捷筛选栏**：标题栏下方常驻"全部/今天/过期/无截止日期"四枚筛选项，点击一键切换视图，再次点击取消。右侧实时显示当前筛选结果数量，筛选状态与搜索面板完全同步。
2. **二级精搜 — 搜索面板**：按 Ctrl/Cmd+F 唤出搜索面板（默认隐藏），提供关键词搜索（匹配文本、备注）、分类过滤（标签从已有任务自动提取）和日期状态筛选（今天到期/逾期/无截止日期/未来到期）三个维度的精确搜索，支持多条件组合。筛选结果与快捷筛选栏联动，实现粗筛后的二次聚焦。

【图片：悬浮窗口快速筛选栏截图——展示标题栏下方"全部/今天/过期/无截止日期"筛选按钮及右侧显示数量】

**步骤五：归档与 AI 自动分类**

TodoFloat 的归档流程实现了任务数据的自动结构化，核心链路如下：

1. **触发归档**：用户对已完成任务右键选择"归档"，主进程更新 `archived` 和 `archived_at` 字段，将任务移入归档库；
2. **分类检查**：归档完成后，主进程异步检查该任务是否已有分类标签，避免重复分类；
3. **AI 自动分类**���若任务尚无分类且用户已配置 API Key，主进程在后台调用 LLM 分类服务：
   - 使用 `temperature: 0.1` 的低随机性参数，确保分类结果的确定性和一致性；
   - System Prompt 定义了 10 个标准工作类别（开发、设计、测试、文档、会议、沟通、运维、学习、管理、其他）；
   - LLM 返回类别名称后，主进程自动写入 `category` 字段；
4. **结果同步**：分类完成后通过 IPC 通知归档窗口刷新数据，用户即可看到自动标注的分类标签。

*此外，用户可在归档页面手动触发"AI 分类"或直接修改分类标签，保持对分类结果的完全控制。*

【图片：历史归档页面截图——展示归档任务列表，包含任务内容、AI 自动标注的类别标签（如"开发""会议""文档"等）、备注、归档时间，以及顶部的搜索/筛选栏和导出按钮】

**步骤六：工作数据分析与可视化**

工作分析模块提供三个时间维度（周/月/年）的数据洞察：

1. **数据聚合**：主进程的 `getWorkAnalysis` 方法根据选定的时间范围，从 SQLite 查询归档任务，按类别和日期进行聚合统计，同时计算全局的完成率（已归档数 / 总任务数）；
2. **前端可视化**：渲染进程使用 Recharts 库绘制类别分布饼图（环形图）和每日任务柱状图，以及类别占比进度条；
3. **AI 智能分析**：数据加载完成后，异步调用 LLM 分析接口。LLM 接收完整的统计数据（任务数量、分类分布、每日分布、完成率等），按照 Prompt 要求生成三个维度的分析——工作重点与时间分配、工作节奏规律、1-3 条优化建议，总字数控制在 500 字以内。结果以 Markdown 格式返回，前端通过 ReactMarkdown 渲染。

【图片：工作分析页面完整截图——展示顶部的周/月/年切换按钮、四张概览卡片（待办任务数、归档任务数、工作类别数、完成率）、左侧类别分布饼图、右侧每日任务柱状图、底部 AI 工作分析建议区域】

**步骤七：主题系统**

TodoFloat 支持浅色、深色、护眼三套视觉主题，通过 CSS 变量 + `data-theme` 属性实现实时切换：

- **浅色模式**（默认）：白色背景，灰色文字；
- **深色模式**：深蓝灰色背景（Catppuccin Mocha 色板），适配暗色环境；
- **护眼模式**：米黄色暖色调背景，降低蓝光刺激。

技术实现上，通过 `document.documentElement.setAttribute('data-theme', theme)` 切换属性，配合 CSS 选择器 `[data-theme="dark"]` 和 `[data-theme="eye-care"]` 覆盖所有 Tailwind 默认样式类，无需引入额外主题库。

【图片：三种主题模式对比截图——并排展示同一悬浮窗口在浅色模式、深色模式、护眼模式下的外观效果】

**步骤八：CI/CD 自动化发布**

TodoFloat 实现了从代码提交到安装包发布的全链路自动化：

1. **版本发布**：开发者执行 `npm run release`，脚本自动更新版本号、提交代码、推送并创建 Git tag；
2. **并行构建**：tag 推送触发 GitHub Actions 工作流，在 macOS 和 Windows 环境上并行构建安装包（.dmg 和 .exe）；
3. **自动发布**：构建完成后自动创建 GitHub Release 并上传安装包。

【图片：CI/CD 发布流程图——展示从 `npm run release` 命令开始，经过版本更新、Git tag 推送、GitHub Actions 触发、macOS/Windows 双平台并行构建、Release 创建的完整流程】

**步骤九：子任务分解**

TodoFloat 支持将任务拆解为多个子任务，实现从"任务管理"到"步骤管理"的细化：

1. **按需创建**：每个 todo 项右侧提供子任务展开按钮，点击展开后可通过内联输入框快速创建子任务，按 Enter 提交（含中文输入法组合状态检测），自动按 sort_order 排序；
2. **进度可视化**：已有子任务的任务项下方自动显示进度条，直观展示"已完成/总数"（X/Y）的完成进度，进度条宽度随比例动态变化；
3. **展开/折叠交互**：子任务列表支持展开/折叠切换，折叠状态下通过进度条依然可见宏观进度，展开后可逐个查看和操作每个子步骤；
4. **内联完成与删除**：每个子任务前有圆形复选框，点击可切换完成/未完成状态，已完成子任务文字以删除线显示并淡化。右侧悬停时出现删除按钮。

技术实现上，子任务数据存储在独立的 `subtasks` 表中（含 todo_id、text、completed、sort_order 字段），通过 `todo_id` 外键与父任务关联。子任务采用懒加载策略（首次展开时从数据库读取），展开/折叠状态由本地 Set 管理，无需额外的路由或全局状态。

【图片：子任务展开视图截图——展示任务下方展开的三个子任务，含完成/未完成圆圈、进度条、底部内联输入框】

**步骤十：番茄钟专注计时**

TodoFloat 内置番茄工作法计时器，将任务执行与专注时间管理深度绑定：

1. **可配置循环**：支持自定义专注时长（默认 25 分钟）、短休息（5 分钟）、长休息（15 分钟）以及长休息触发间隔（每完成 4 个专注周期触发一次长休息），配置项通过系统托盘—设置页面调整，存储在 SQLite settings 表；
2. **任务关联**：启动专注前可从活跃任务列表中关联目标任务（可选），专注过程中顶部提示栏实时显示关联任务名称，结束后在通知和 session 记录中保留任务关联信息；
3. **SVG 进度环**：展开面板后显示圆形进度环（stroke-dasharray 动画），随时间逐渐填充，专注周期为红色（#f43f5e），休息周期为绿色（#10b981）。中心显示剩余时间（MM:SS）及当前周期类型标签；
4. **OS 原生通知**：每个周期结束时，通过 Electron Notification API 发送系统级桌面通知。专注结束通知附带关联任务信息，用户点击通知可直接激活悬浮窗；
5. **自动循环**：专注结束后自动进入短休息，短休息结束后自动回到专注状态（可手动暂停/继续/停止），每完成 4 个专注周期后触发一次长休息。整个循环由主进程的 setInterval 驱动，状态通过 IPC `pomodoro:stateChanged` 事件广播到所有渲染进程窗口，保证悬浮窗和归档分析页面状态一致；
6. **统计数据持久化**：每次专注和休息均在 `pomodoro_sessions` 表中创建 session 记录，含开始时间、计划时长、实际执行时长、完成状态、周期类型。统计数据（今日/本周/本月完成数、总专注时长）在工作分析页面以图表形式呈现，与归档任务分析并列展示。

技术实现上，番茄钟状态机由主进程集中管理（running / paused / idle），通过 IPC 事件向所有渲染进程窗口广播状态变更。定时器使用 setInterval 每秒递减计数器，sessions 表记录每次专注和休息的实际起止时间，支撑后续的效率统计分析。

【图片：番茄钟展开面板截图——展示圆形进度环、任务选择器下拉框、周期指示点、开始专注按钮】

【图片：番茄钟统计截图——展示工作分析页面的番茄统计区域，含今日完成/总完成/总专注分钟数、每日分布柱状图、最近记录列表】

## 三、创新亮点

1. **AI 驱动的智能任务管理闭环**：在任务归档环节引入大语言模型，实现对任务文本的自动类别标注（temperature 0.1，预定义 10 个标准工作类别），免去用户手动打标签的成本，让历史数据自动结构化。在此基础上，将归档数据聚合为类别分布、时间分布、完成率等多维度统计信息，作为上下文传递给 LLM，生成包含工作重点分析、节奏规律识别和优化建议三个维度的个性化效率分析报告。同时通过 OpenAI 兼容协议实现了模型可替换性，仅需配置 API Key、Base URL 和模型名称即可接入主流大模型服务，保持 AI 能力的灵活演进。

2. **零摩擦交互设计**：通过"始终可见 + 就地操作 + 快速检索"三重设计，将任务管理的交互成本降至最低。悬浮窗口以透明无框形态驻留在所有应用之上，用户无需切换窗口即可随手记录任务。任务文本支持双击就地编辑，子任务通过内联输入框按需创建，颜色标签一键循环切换，操作路径精简到极致。结合常驻的快捷筛选栏（全部/今天/过期/无截止日期）、Ctrl+F 关键词搜索面板、已完成任务自动折叠、以及软删除 5 秒可撤销机制，用户在任何场景下都能快速找到目标任务并完成操作，且不用担心误删。

3. **"记录到洞察"的工作方法论闭环**：TodoFloat 重新定义了待办工具的角色——从被动的"记录者"升级为主动的"洞察者"。在方法流程层面，构建了"任务记录→子任务拆解→番茄钟执行→归档分类→数据分析→优化建议"的完整闭环。每个环节的数据自然流转至下一阶段：记录的任务通过子任务分解细化执行步骤，番茄钟计时保障专注执行，归档时 AI 自动分类结构化存储数据，最终通过多维度统计和 AI 分析输出可操作的工作效率优化建议。这一闭环将碎片化的任务管理行为转化为持续驱动个人效率提升的系统性方法论。
